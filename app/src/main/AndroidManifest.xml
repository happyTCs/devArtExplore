<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xu.walt.com.aidl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SecordActivity"/>
        <service
            android:process=":remote"
            android:name=".MyService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="xu.walt.com.aidl.MyService"/>
            </intent-filter>
        </service>

        <service android:name=".LongRunningService">
           <!-- <intent-filter >
                <action android:name="xu.walt.com.aidl.LongRunningService"/>
            </intent-filter>-->
        </service>
        <receiver android:name=".broadcastReceiver.ScreenControlAlarmReceiver">
            <intent-filter>
                <action android:name="intent_alarm_log"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>